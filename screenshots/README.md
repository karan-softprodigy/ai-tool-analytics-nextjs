Run `npm run dev` and capture the following views into this folder:

- overview.png — KPI cards, usage chart, model breakdown, insights feed
- query-logs.png — filterable request log table
- models.png — model comparison cards
- mobile-overview.png — overview page at a mobile viewport width
